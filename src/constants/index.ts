import colors from './colors';
import routes from './routes';
import env from './env';
import errorMessages from './errorMessages';
import { sessionStorage, localStorage, location } from './globals';

export {
  colors,
  routes,
  env,
  sessionStorage,
  localStorage,
  location,
  errorMessages,
};
